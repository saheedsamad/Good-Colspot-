<!-- 🔹 STUDENT CLASS ACTIVITY SCREEN START -->
<div id="screen-reports" class="screen">
  <style>
    #screen-reports{
      padding:8px 6px 96px;
      overflow-y:auto;
      -webkit-overflow-scrolling:touch;
    }
    #screen-reports .hero{
      padding:4px 4px 6px;
      margin-bottom:6px;
    }
    #screen-reports .hero-line-1{
      font-size:12px;
      font-weight:500;
      color:var(--text-muted);
      margin-bottom:3px;
    }
    #screen-reports .hero-title{
      font-size:20px;
      font-weight:800;
      letter-spacing:-0.03em;
      line-height:1.2;
      color:var(--text-main);
      margin:0;
    }
    #screen-reports .hero-title span{
      color:var(--blue);
      font-weight:600;
    }
    #screen-reports .hero-sub{
      font-size:12px;
      color:var(--text-muted);
      margin:6px 0 0;
      line-height:1.45;
    }
    #screen-reports .activities-top-card{
      margin:6px 2px 14px;
      border-radius:18px;
      background:linear-gradient(135deg,#eff6ff,#ffffff);
      border:1px solid #dbeafe;
      padding:14px 14px 12px;
      box-shadow:0 10px 30px rgba(15,23,42,.06);
    }
    #screen-reports .activities-top-title{
      font-size:15px;
      font-weight:700;
      color:var(--text-main);
      margin-bottom:4px;
    }
    #screen-reports .activities-top-sub{
      font-size:12px;
      color:var(--text-muted);
      line-height:1.4;
    }
    #screen-reports .activity-list{
      display:flex;
      flex-direction:column;
      gap:10px;
      margin-top:10px;
    }
    #screen-reports .activity-item{
      border-radius:18px;
      background:#ffffff;
      border:1px solid #e5e7eb;
      padding:12px;
      box-shadow:0 10px 30px rgba(15,23,42,.06);
      cursor:pointer;
      transition:transform .05s, box-shadow .15s;
    }
    #screen-reports .activity-item:active{
      transform:scale(.99);
    }
    #screen-reports .activity-item-top{
      display:flex;
      justify-content:space-between;
      align-items:flex-start;
      gap:10px;
      margin-bottom:8px;
    }
    #screen-reports .activity-course{
      font-size:13px;
      font-weight:700;
      color:var(--text-main);
      margin-bottom:3px;
    }
    #screen-reports .activity-title{
      font-size:15px;
      font-weight:700;
      color:var(--text-main);
      line-height:1.3;
    }
    #screen-reports .activity-badge{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      border-radius:999px;
      padding:4px 10px;
      font-size:11px;
      font-weight:700;
      white-space:nowrap;
    }
    #screen-reports .activity-badge.revision{
      background:#eff6ff;
      color:#2563eb;
    }
    #screen-reports .activity-badge.assignment{
      background:#f5f3ff;
      color:#6d28d9;
    }
    #screen-reports .activity-badge.test{
      background:#fef3c7;
      color:#b45309;
    }
    #screen-reports .activity-meta-row{
      display:flex;
      flex-wrap:wrap;
      gap:8px;
      margin-bottom:10px;
    }
    #screen-reports .activity-meta-pill{
      display:inline-flex;
      align-items:center;
      gap:5px;
      border-radius:999px;
      padding:5px 9px;
      font-size:11px;
      background:#f8fafc;
      border:1px solid #e5e7eb;
      color:var(--text-muted);
    }
    #screen-reports .activity-status-row{
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:10px;
    }
    #screen-reports .activity-status{
      font-size:12px;
      font-weight:600;
    }
    #screen-reports .activity-status.not-started{
      color:#15803d;
    }
    #screen-reports .activity-status.in-progress{
      color:#2563eb;
    }
    #screen-reports .activity-status.submitted{
      color:#6b7280;
    }
    #screen-reports .activity-open-btn{
      border:none;
      border-radius:999px;
      padding:8px 14px;
      font-size:12px;
      font-weight:700;
      color:#ffffff;
      background:linear-gradient(135deg,#2563eb,#1d4ed8);
      cursor:pointer;
      box-shadow:var(--shadow-soft);
    }
    #screen-reports .activity-open-btn.disabled{
      background:#cbd5e1;
      box-shadow:none;
      cursor:default;
    }
    #screen-reports .activity-empty{
      border-radius:18px;
      background:#ffffff;
      border:1px solid #e5e7eb;
      padding:16px 14px;
      font-size:12px;
      color:var(--text-muted);
      text-align:center;
      box-shadow:0 10px 30px rgba(15,23,42,.06);
    }
    #screen-reports .activity-intro-card,
    #screen-reports .activity-question-card,
    #screen-reports .activity-review-card,
    #screen-reports .activity-submitted-card{
      border-radius:18px;
      background:#ffffff;
      border:1px solid #e5e7eb;
      padding:14px;
      box-shadow:0 10px 30px rgba(15,23,42,.06);
      margin:8px 2px 14px;
    }
    #screen-reports .activity-back-btn{
      width:100%;
      margin-bottom:10px;
      border-radius:999px;
      border:1px solid #e5e7eb;
      background:#ffffff;
      color:var(--text-main);
      padding:10px 12px;
      font-size:13px;
      font-weight:600;
      cursor:pointer;
    }
    #screen-reports .activity-intro-title,
    #screen-reports .activity-review-title,
    #screen-reports .activity-submitted-title{
      font-size:18px;
      font-weight:700;
      color:var(--text-main);
      margin-bottom:8px;
    }
    #screen-reports .activity-intro-course{
      font-size:14px;
      font-weight:700;
      color:var(--text-main);
      margin-bottom:10px;
    }
    #screen-reports .activity-intro-meta{
      display:flex;
      flex-direction:column;
      gap:8px;
      margin-bottom:14px;
    }
    #screen-reports .activity-intro-meta-item{
      font-size:13px;
      color:var(--text-main);
      display:flex;
      justify-content:space-between;
      gap:10px;
      padding-bottom:8px;
      border-bottom:1px solid #f1f5f9;
    }
    #screen-reports .activity-warning-box{
      border-radius:14px;
      background:#eff6ff;
      border:1px solid #dbeafe;
      color:#1e3a8a;
      padding:12px;
      font-size:12px;
      line-height:1.45;
      margin-bottom:12px;
    }
    #screen-reports .activity-progress-top{
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:10px;
      margin-bottom:8px;
      font-size:12px;
      color:var(--text-main);
      font-weight:600;
    }
    #screen-reports .activity-progress-track{
      width:100%;
      height:6px;
      border-radius:999px;
      background:#e5e7eb;
      overflow:hidden;
      margin-bottom:14px;
    }
    #screen-reports .activity-progress-bar{
      height:100%;
      border-radius:999px;
      background:linear-gradient(135deg,#2563eb,#1d4ed8);
      width:0%;
    }
    #screen-reports .activity-question-title{
      font-size:17px;
      font-weight:700;
      line-height:1.4;
      color:var(--text-main);
      margin-bottom:14px;
    }
    #screen-reports .activity-option-list{
      display:flex;
      flex-direction:column;
      gap:10px;
      margin-bottom:14px;
    }
    #screen-reports .activity-option{
      border-radius:14px;
      border:1px solid #e5e7eb;
      background:#ffffff;
      padding:12px 12px;
      display:flex;
      align-items:flex-start;
      gap:10px;
      cursor:pointer;
    }
    #screen-reports .activity-option.active{
      border-color:#2563eb;
      background:#eff6ff;
      box-shadow:0 0 0 1px rgba(37,99,235,.2);
    }
    #screen-reports .activity-option-letter{
      width:28px;
      height:28px;
      border-radius:8px;
      background:#f3f4f6;
      color:var(--text-main);
      font-size:13px;
      font-weight:700;
      display:flex;
      align-items:center;
      justify-content:center;
      flex-shrink:0;
    }
    #screen-reports .activity-option.active .activity-option-letter{
      background:#dbeafe;
      color:#1d4ed8;
    }
    #screen-reports .activity-option-text{
      font-size:14px;
      line-height:1.45;
      color:var(--text-main);
      flex:1;
    }
    #screen-reports .activity-nav-row{
      display:flex;
      gap:10px;
    }
    #screen-reports .activity-nav-row button{
      flex:1;
    }
    #screen-reports .activity-ghost-btn{
      border-radius:999px;
      border:1px solid #d1d5db;
      padding:11px 14px;
      font-size:14px;
      font-weight:600;
      background:#ffffff;
      color:var(--text-main);
      cursor:pointer;
    }
    #screen-reports .activity-review-list{
      display:flex;
      flex-direction:column;
      gap:10px;
      margin-bottom:14px;
    }
    #screen-reports .activity-review-item{
      border-radius:14px;
      background:#f9fafb;
      border:1px solid #e5e7eb;
      padding:12px;
      cursor:pointer;
    }
    #screen-reports .activity-review-q{
      font-size:13px;
      font-weight:700;
      color:var(--text-main);
      margin-bottom:8px;
    }
    #screen-reports .activity-review-a{
      font-size:13px;
      color:var(--text-muted);
      line-height:1.4;
    }
    #screen-reports .activity-review-a strong{
      color:var(--text-main);
    }
    #screen-reports .activity-submit-note{
      font-size:12px;
      color:var(--text-muted);
      line-height:1.45;
      margin-bottom:12px;
    }
    #screen-reports .activity-success-icon{
      width:78px;
      height:78px;
      border-radius:999px;
      background:#22c55e;
      color:#ffffff;
      display:flex;
      align-items:center;
      justify-content:center;
      margin:6px auto 14px;
      box-shadow:0 12px 30px rgba(34,197,94,.25);
    }
    #screen-reports .activity-success-icon svg{
      width:38px;
      height:38px;
    }
    #screen-reports .activity-submitted-text{
      font-size:14px;
      color:var(--text-main);
      text-align:center;
      line-height:1.5;
      margin-bottom:14px;
    }
    #screen-reports .activity-submitted-box{
      border-radius:14px;
      background:#eff6ff;
      border:1px solid #dbeafe;
      color:#1e3a8a;
      padding:12px;
      font-size:12px;
      line-height:1.45;
      margin-bottom:14px;
      text-align:center;
    }
    #screen-reports .activity-loading{
      font-size:12px;
      color:var(--text-muted);
      text-align:center;
      padding:14px 12px;
    }
    #screen-reports .hidden{
      display:none !important;
    }
    @media (max-width:360px){
      #screen-reports .activity-nav-row{
        flex-direction:column;
      }
    }
  </style>

  <!-- LIST -->
  <section id="studentActivitiesListView">
    <section class="hero">
      <div class="hero-line-1">Class activity</div>
      <h1 class="hero-title">
        Answer your <span>class questions</span>
      </h1>
      <p class="hero-sub">
        Revisions, tests, and assignments from classes you already joined.
      </p>
    </section>

    <article class="activities-top-card">
      <div class="activities-top-title">Your class activities</div>
      <div class="activities-top-sub">
        Tap any published activity to open it. Once you submit, you cannot retry again.
      </div>
    </article>

    <div id="studentActivitiesList" class="activity-list">
      <div class="activity-empty">Loading class activities...</div>
    </div>
  </section>

  <!-- INTRO -->
  <section id="studentActivityIntroView" class="hidden">
    <button class="activity-back-btn" type="button" onclick="studentBackToActivitiesList()">← Back to activities</button>

    <article class="activity-intro-card">
      <div class="activity-intro-title" id="studentActivityIntroTitle">Activity</div>
      <div class="activity-intro-course" id="studentActivityIntroCourse">Class</div>

      <div class="activity-intro-meta">
        <div class="activity-intro-meta-item">
          <span>Questions</span>
          <strong id="studentActivityIntroQuestions">0 Questions</strong>
        </div>
        <div class="activity-intro-meta-item">
          <span>Time limit</span>
          <strong id="studentActivityIntroTime">No limit</strong>
        </div>
        <div class="activity-intro-meta-item">
          <span>Due</span>
          <strong id="studentActivityIntroDue">No due date</strong>
        </div>
      </div>

      <div class="activity-warning-box">
        You can only submit once. Your score will not show immediately. You will only see confirmation after submission.
      </div>

      <button id="studentStartActivityBtn" class="primary-cta" type="button" onclick="studentStartActivity()">Start activity</button>
    </article>
  </section>

  <!-- QUESTIONS -->
  <section id="studentActivityQuestionView" class="hidden">
    <button class="activity-back-btn" type="button" onclick="studentGoBackQuestion()">← Back</button>

    <article class="activity-question-card">
      <div class="activity-progress-top">
        <span id="studentActivityProgressText">Question 1 of 1</span>
        <span id="studentActivityProgressPercent">100%</span>
      </div>

      <div class="activity-progress-track">
        <div id="studentActivityProgressBar" class="activity-progress-bar"></div>
      </div>

      <div id="studentActivityQuestionTitle" class="activity-question-title">Question</div>

      <div id="studentActivityOptionList" class="activity-option-list"></div>

      <div class="activity-nav-row">
        <button id="studentActivityPrevBtn" class="activity-ghost-btn" type="button" onclick="studentGoPrevQuestion()">Back</button>
        <button id="studentActivityNextBtn" class="primary-cta" type="button" onclick="studentGoNextQuestion()">Next</button>
      </div>
    </article>
  </section>

  <!-- REVIEW -->
  <section id="studentActivityReviewView" class="hidden">
    <button class="activity-back-btn" type="button" onclick="studentBackToLastQuestion()">← Back to questions</button>

    <article class="activity-review-card">
      <div class="activity-review-title">Review your answers</div>
      <div class="activity-submit-note">
        Check all your selected answers below before submitting. Tap any item if you want to go back and change it.
      </div>

      <div id="studentActivityReviewList" class="activity-review-list"></div>

      <button id="studentSubmitActivityBtn" class="primary-cta" type="button" onclick="studentSubmitActivity()">Submit activity</button>
    </article>
  </section>

  <!-- SUBMITTED -->
  <section id="studentActivitySubmittedView" class="hidden">
    <article class="activity-submitted-card">
      <div class="activity-success-icon">
        <svg viewBox="0 0 24 24" fill="none">
          <path d="M5 13l4 4L19 7" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>

      <div class="activity-submitted-title" style="text-align:center;">Activity submitted</div>

      <div class="activity-submitted-text">
        Your answers have been submitted successfully.
      </div>

      <div class="activity-submitted-box">
        You cannot retry this activity again. Your result will only show after the lecturer releases it.
      </div>

      <button class="primary-cta" type="button" onclick="studentBackToActivitiesList()">Back to activities</button>
    </article>
  </section>

  <script>
    window.studentClassActivityState = window.studentClassActivityState || {
      activities: [],
      currentActivity: null,
      questions: [],
      answers: {},
      currentIndex: 0,
      currentAttemptId: null,
      loading: false
    };

    function studentActivityEscape(value) {
      return String(value ?? "")
        .replaceAll("&", "&amp;")
        .replaceAll("<", "&lt;")
        .replaceAll(">", "&gt;")
        .replaceAll('"', "&quot;")
        .replaceAll("'", "&#039;");
    }

    function studentFmtDate(value) {
      if (!value) return "No due date";
      try {
        const d = new Date(value);
        if (Number.isNaN(d.getTime())) return "No due date";
        return d.toLocaleString([], {
          year: "numeric",
          month: "short",
          day: "numeric",
          hour: "numeric",
          minute: "2-digit"
        });
      } catch {
        return "No due date";
      }
    }

    function studentShowActivityView(viewName) {
      const ids = [
        "studentActivitiesListView",
        "studentActivityIntroView",
        "studentActivityQuestionView",
        "studentActivityReviewView",
        "studentActivitySubmittedView"
      ];

      ids.forEach((id) => {
        const el = document.getElementById(id);
        if (!el) return;
        el.classList.toggle("hidden", id !== viewName);
      });
    }

    async function studentEnsureUser() {
      if (typeof ensureCurrentUser === "function") {
        return await ensureCurrentUser();
      }

      if (window.currentUser) return window.currentUser;
      if (typeof supabase === "undefined") return null;

      try {
        const { data, error } = await supabase.auth.getUser();
        if (error || !data?.user) return null;
        window.currentUser = data.user;
        return data.user;
      } catch {
        return null;
      }
    }

    async function studentLoadActivitiesScreen() {
      const wrap = document.getElementById("studentActivitiesList");
      if (!wrap) return;

      wrap.innerHTML = `<div class="activity-empty">Loading class activities...</div>`;

      if (typeof supabase === "undefined") {
        wrap.innerHTML = `<div class="activity-empty">Could not connect to the server.</div>`;
        return;
      }

      const user = await studentEnsureUser();
      if (!user) {
        wrap.innerHTML = `<div class="activity-empty">Please sign in again to load your activities.</div>`;
        return;
      }

      try {
        const { data: memberships, error: membershipError } = await supabase
          .from("course_memberships")
          .select("course_id")
          .eq("user_id", user.id);

        if (membershipError) throw membershipError;

        const classIds = (memberships || []).map(x => x.course_id).filter(Boolean);

        if (!classIds.length) {
          window.studentClassActivityState.activities = [];
          wrap.innerHTML = `<div class="activity-empty">You have not joined any class activity yet.</div>`;
          return;
        }

        const { data: courses, error: coursesError } = await supabase
          .from("courses")
          .select("id, course_code, course_title")
          .in("id", classIds);

        if (coursesError) throw coursesError;

        const courseMap = {};
        (courses || []).forEach((c) => {
          courseMap[c.id] = c;
        });

        const { data: activities, error: activitiesError } = await supabase
          .from("class_activities")
          .select("id, class_id, activity_type, title, due_at, time_limit_minutes, attempts_allowed, is_published, status, created_at")
          .in("class_id", classIds)
          .eq("is_published", true)
          .eq("status", "published")
          .order("created_at", { ascending: false });

        if (activitiesError) throw activitiesError;

        const activityIds = (activities || []).map(a => a.id).filter(Boolean);

        let attempts = [];
        if (activityIds.length) {
          const { data: attemptsData, error: attemptsError } = await supabase
            .from("class_activity_attempts")
            .select("id, activity_id, started_at, submitted_at, status")
            .eq("user_id", user.id)
            .in("activity_id", activityIds);

          if (attemptsError) throw attemptsError;
          attempts = attemptsData || [];
        }

        let questionRows = [];
        if (activityIds.length) {
          const { data: qs, error: qsError } = await supabase
            .from("class_activity_questions")
            .select("id, activity_id")
            .in("activity_id", activityIds);

          if (qsError) throw qsError;
          questionRows = qs || [];
        }

        const countMap = {};
        questionRows.forEach((q) => {
          const key = q.activity_id;
          countMap[key] = (countMap[key] || 0) + 1;
        });

        const attemptMap = {};
        attempts.forEach((a) => {
          attemptMap[a.activity_id] = a;
        });

        const items = (activities || []).map((a) => {
          const attempt = attemptMap[a.id] || null;
          let status = "not-started";
          if (attempt?.status === "submitted" || attempt?.submitted_at) {
            status = "submitted";
          } else if (attempt) {
            status = "in-progress";
          }

          const course = courseMap[a.class_id] || {};

          return {
            id: a.id,
            class_id: a.class_id,
            class_code: course.course_code || "Class",
            class_title: course.course_title || "",
            type: a.activity_type || "revision",
            title: a.title || "Untitled activity",
            questions_count: countMap[a.id] || 0,
            time_limit: a.time_limit_minutes || null,
            due_at: a.due_at || null,
            due_text: studentFmtDate(a.due_at),
            status,
            attempt_id: attempt?.id || null
          };
        });

        window.studentClassActivityState.activities = items;

        if (!items.length) {
          wrap.innerHTML = `<div class="activity-empty">No class activity available yet.</div>`;
          return;
        }

        wrap.innerHTML = items.map((item) => {
          const typeClass = item.type || "revision";
          const typeLabel =
            typeClass === "assignment" ? "Assignment" :
            typeClass === "test" ? "Test" : "Revision";

          const statusClass =
            item.status === "submitted" ? "submitted" :
            item.status === "in-progress" ? "in-progress" : "not-started";

          const statusLabel =
            item.status === "submitted" ? "Submitted" :
            item.status === "in-progress" ? "In progress" : "Not started";

          const isSubmitted = item.status === "submitted";

          return `
            <article class="activity-item" onclick="${isSubmitted ? "" : `studentOpenActivityIntro('${item.id}')`}">
              <div class="activity-item-top">
                <div>
                  <div class="activity-course">${studentActivityEscape(item.class_code)}</div>
                  <div class="activity-title">${studentActivityEscape(item.title)}</div>
                </div>
                <div class="activity-badge ${typeClass}">${typeLabel}</div>
              </div>

              <div class="activity-meta-row">
                <div class="activity-meta-pill">${studentActivityEscape(item.questions_count)} Questions</div>
                <div class="activity-meta-pill">${studentActivityEscape(item.due_text)}</div>
              </div>

              <div class="activity-status-row">
                <div class="activity-status ${statusClass}">${statusLabel}</div>
                <button class="activity-open-btn ${isSubmitted ? "disabled" : ""}" type="button">
                  ${item.status === "in-progress" ? "Continue" : isSubmitted ? "Submitted" : "Open"}
                </button>
              </div>
            </article>
          `;
        }).join("");
      } catch (err) {
        console.error("studentLoadActivitiesScreen failed:", err);
        wrap.innerHTML = `<div class="activity-empty">Could not load activities right now.</div>`;
      }
    }

    async function studentOpenActivityIntro(activityId) {
      const found = (window.studentClassActivityState.activities || []).find((x) => x.id === activityId);
      if (!found) return;

      document.getElementById("studentActivityIntroTitle").textContent = found.title;
      document.getElementById("studentActivityIntroCourse").textContent = `${found.class_code} - ${found.class_title}`;
      document.getElementById("studentActivityIntroQuestions").textContent = `${found.questions_count} Questions`;
      document.getElementById("studentActivityIntroTime").textContent = found.time_limit ? `${found.time_limit} Minutes` : "No limit";
      document.getElementById("studentActivityIntroDue").textContent = found.due_text || "No due date";

      window.studentClassActivityState.currentActivity = found;
      window.studentClassActivityState.questions = [];
      window.studentClassActivityState.answers = {};
      window.studentClassActivityState.currentIndex = 0;
      window.studentClassActivityState.currentAttemptId = found.attempt_id || null;

      studentShowActivityView("studentActivityIntroView");
    }

    function studentBackToActivitiesList() {
      studentShowActivityView("studentActivitiesListView");
      studentLoadActivitiesScreen();
    }

    async function studentEnsureAttempt() {
      const activity = window.studentClassActivityState.currentActivity;
      if (!activity) return null;

      const user = await studentEnsureUser();
      if (!user) return null;

      if (window.studentClassActivityState.currentAttemptId) {
        return window.studentClassActivityState.currentAttemptId;
      }

      const { data: existing, error: existingError } = await supabase
        .from("class_activity_attempts")
        .select("id, status, submitted_at")
        .eq("activity_id", activity.id)
        .eq("user_id", user.id)
        .maybeSingle();

      if (existingError) throw existingError;

      if (existing) {
        if (existing.status === "submitted" || existing.submitted_at) {
          window.studentClassActivityState.currentAttemptId = existing.id;
          return existing.id;
        }
        window.studentClassActivityState.currentAttemptId = existing.id;
        return existing.id;
      }

      const { data: created, error: createError } = await supabase
        .from("class_activity_attempts")
        .insert([{
          activity_id: activity.id,
          user_id: user.id,
          status: "in_progress",
          started_at: new Date().toISOString()
        }])
        .select("id")
        .single();

      if (createError) throw createError;

      window.studentClassActivityState.currentAttemptId = created.id;
      return created.id;
    }

    async function studentLoadAnswersForAttempt(attemptId) {
      if (!attemptId) return {};

      const { data, error } = await supabase
        .from("class_activity_answers")
        .select("question_id, selected_option")
        .eq("attempt_id", attemptId);

      if (error) throw error;

      const answers = {};
      (data || []).forEach((row) => {
        answers[row.question_id] = row.selected_option;
      });
      return answers;
    }

    async function studentStartActivity() {
      if (typeof supabase === "undefined") return;
      const activity = window.studentClassActivityState.currentActivity;
      if (!activity) return;

      const btn = document.getElementById("studentStartActivityBtn");
      const oldText = btn.textContent;
      btn.disabled = true;
      btn.textContent = "Starting...";

      try {
        const { data: questions, error: questionError } = await supabase
          .from("class_activity_questions")
          .select("id, question_text, option_a, option_b, option_c, option_d, position")
          .eq("activity_id", activity.id)
          .order("position", { ascending: true });

        if (questionError) throw questionError;

        window.studentClassActivityState.questions = (questions || []).map((q) => ({
          id: q.id,
          question: q.question_text,
          options: {
            A: q.option_a,
            B: q.option_b,
            C: q.option_c,
            D: q.option_d
          },
          position: q.position
        }));

        const attemptId = await studentEnsureAttempt();
        const savedAnswers = await studentLoadAnswersForAttempt(attemptId);

        window.studentClassActivityState.answers = savedAnswers || {};
        window.studentClassActivityState.currentIndex = 0;

        studentRenderQuestion();
        studentShowActivityView("studentActivityQuestionView");
      } catch (err) {
        console.error("studentStartActivity failed:", err);
        alert("Could not open this activity right now.");
      } finally {
        btn.disabled = false;
        btn.textContent = oldText;
      }
    }

    function studentRenderQuestion() {
      const questions = window.studentClassActivityState.questions;
      const idx = window.studentClassActivityState.currentIndex;
      const q = questions[idx];
      if (!q) return;

      const total = questions.length;
      const progress = Math.round(((idx + 1) / total) * 100);

      document.getElementById("studentActivityProgressText").textContent = `Question ${idx + 1} of ${total}`;
      document.getElementById("studentActivityProgressPercent").textContent = `${progress}%`;
      document.getElementById("studentActivityProgressBar").style.width = `${progress}%`;
      document.getElementById("studentActivityQuestionTitle").textContent = q.question;

      const wrap = document.getElementById("studentActivityOptionList");
      const selected = window.studentClassActivityState.answers[q.id] || "";

      wrap.innerHTML = Object.entries(q.options).map(([key, value]) => `
        <div class="activity-option ${selected === key ? "active" : ""}" onclick="studentSelectOption('${q.id}','${key}')">
          <div class="activity-option-letter">${key}</div>
          <div class="activity-option-text">${studentActivityEscape(value)}</div>
        </div>
      `).join("");

      document.getElementById("studentActivityPrevBtn").style.visibility = idx === 0 ? "hidden" : "visible";
      document.getElementById("studentActivityNextBtn").textContent = idx === total - 1 ? "Review answers" : "Next";
    }

    async function studentSelectOption(questionId, optionKey) {
      window.studentClassActivityState.answers[questionId] = optionKey;
      studentRenderQuestion();

      try {
        const attemptId = await studentEnsureAttempt();
        if (!attemptId) return;

        const { data: existing, error: existingError } = await supabase
          .from("class_activity_answers")
          .select("id")
          .eq("attempt_id", attemptId)
          .eq("question_id", questionId)
          .maybeSingle();

        if (existingError) throw existingError;

        if (existing?.id) {
          const { error: updateError } = await supabase
            .from("class_activity_answers")
            .update({ selected_option: optionKey })
            .eq("id", existing.id);

          if (updateError) throw updateError;
        } else {
          const { error: insertError } = await supabase
            .from("class_activity_answers")
            .insert([{
              attempt_id: attemptId,
              question_id: questionId,
              selected_option: optionKey
            }]);

          if (insertError) throw insertError;
        }
      } catch (err) {
        console.error("studentSelectOption save failed:", err);
      }
    }

    function studentGoPrevQuestion() {
      if (window.studentClassActivityState.currentIndex > 0) {
        window.studentClassActivityState.currentIndex -= 1;
        studentRenderQuestion();
      }
    }

    function studentGoBackQuestion() {
      if (window.studentClassActivityState.currentIndex > 0) {
        studentGoPrevQuestion();
      } else {
        studentShowActivityView("studentActivityIntroView");
      }
    }

    function studentGoNextQuestion() {
      const questions = window.studentClassActivityState.questions;
      const idx = window.studentClassActivityState.currentIndex;
      const q = questions[idx];
      if (!q) return;

      if (!window.studentClassActivityState.answers[q.id]) {
        alert("Please choose an answer before continuing.");
        return;
      }

      if (idx === questions.length - 1) {
        studentOpenReviewAnswers();
        return;
      }

      window.studentClassActivityState.currentIndex += 1;
      studentRenderQuestion();
    }

    function studentBackToLastQuestion() {
      studentShowActivityView("studentActivityQuestionView");
      window.studentClassActivityState.currentIndex = window.studentClassActivityState.questions.length - 1;
      studentRenderQuestion();
    }

    function studentJumpToQuestion(index) {
      window.studentClassActivityState.currentIndex = index;
      studentShowActivityView("studentActivityQuestionView");
      studentRenderQuestion();
    }

    function studentOpenReviewAnswers() {
      const wrap = document.getElementById("studentActivityReviewList");
      wrap.innerHTML = window.studentClassActivityState.questions.map((q, index) => {
        const picked = window.studentClassActivityState.answers[q.id];
        const pickedText = picked ? `${picked}. ${q.options[picked]}` : "No answer selected";

        return `
          <div class="activity-review-item" onclick="studentJumpToQuestion(${index})">
            <div class="activity-review-q">Question ${index + 1}: ${studentActivityEscape(q.question)}</div>
            <div class="activity-review-a"><strong>Your answer:</strong> ${studentActivityEscape(pickedText)}</div>
          </div>
        `;
      }).join("");

      studentShowActivityView("studentActivityReviewView");
    }

    async function studentSubmitActivity() {
      const btn = document.getElementById("studentSubmitActivityBtn");
      const oldText = btn.textContent;
      btn.disabled = true;
      btn.textContent = "Submitting...";

      try {
        const attemptId = await studentEnsureAttempt();
        if (!attemptId) throw new Error("Missing attempt");

        const { error } = await supabase
          .from("class_activity_attempts")
          .update({
            status: "submitted",
            submitted_at: new Date().toISOString()
          })
          .eq("id", attemptId);

        if (error) throw error;

        studentShowActivityView("studentActivitySubmittedView");
      } catch (err) {
        console.error("studentSubmitActivity failed:", err);
        alert("Could not submit activity right now.");
      } finally {
        btn.disabled = false;
        btn.textContent = oldText;
      }
    }

    (function bindStudentActivityScreen() {
      const originalShowScreen = window.showScreen;

      if (!window.__studentActivityShowScreenWrapped && typeof originalShowScreen === "function") {
        window.showScreen = function(name) {
          originalShowScreen(name);
          if (name === "reports") {
            studentBackToActivitiesList();
          }
        };
        window.__studentActivityShowScreenWrapped = true;
      }

      document.addEventListener("DOMContentLoaded", () => {
        studentLoadActivitiesScreen();
      });
    })();
  </script>
</div>
<!-- 🔹 STUDENT CLASS ACTIVITY SCREEN END -->
