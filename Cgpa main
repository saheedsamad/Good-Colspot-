<div id="screen-reports" class="screen">
  <style>
    #screen-reports {
      padding: 8px 6px 90px;
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
    }

    #screen-reports .hero {
      padding: 4px 4px 6px;
      margin-bottom: 6px;
    }

    #screen-reports .hero-line-1 {
      font-size: 12px;
      font-weight: 500;
      color: var(--text-muted);
      margin-bottom: 3px;
    }

    #screen-reports .hero-title {
      font-size: 20px;
      font-weight: 800;
      letter-spacing: -0.03em;
      line-height: 1.2;
      color: var(--text-main);
      margin: 0;
    }

    #screen-reports .hero-title span {
      color: var(--blue);
      font-weight: 600;
    }

    #screen-reports .section-title {
      font-size: 13px;
      font-weight: 600;
      color: var(--text-main);
      margin: 8px 2px 4px;
    }

    #screen-reports .section-sub {
      font-size: 12px;
      color: var(--text-muted);
      margin: 0 2px 8px;
      line-height: 1.45;
    }

    #screen-reports .card {
      background: #f9fafb;
      border-radius: 18px;
      padding: 10px 10px;
      border: 1px solid #e5e7eb;
      margin-bottom: 14px;
      box-shadow: 0 10px 30px rgba(15, 23, 42, 0.06);
    }

    #screen-reports .summary-card {
      border-radius: 22px;
      overflow: hidden;
      border: 1px solid #e5e7eb;
      background: #fff;
      margin-bottom: 12px;
      box-shadow: 0 14px 35px rgba(15, 23, 42, 0.08);
    }

    #screen-reports .summary-top {
      background: linear-gradient(135deg, #f59e0b, #fde68a);
      padding: 12px;
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 10px;
      align-items: end;
    }

    #screen-reports .summary-top-label {
      font-size: 12px;
      font-weight: 700;
      color: #fff;
      margin-bottom: 4px;
    }

    #screen-reports .summary-top-value {
      font-size: 28px;
      font-weight: 800;
      color: #fff;
      line-height: 1;
    }

    #screen-reports .summary-standing {
      font-size: 13px;
      font-weight: 700;
      color: #065f46;
      background: rgba(255,255,255,0.78);
      border-radius: 999px;
      padding: 8px 12px;
      align-self: center;
      white-space: nowrap;
    }

    #screen-reports .summary-bottom {
      display: grid;
      grid-template-columns: 1fr 1fr;
      border-top: 1px solid #e5e7eb;
      background: #fff;
    }

    #screen-reports .summary-bottom-item {
      padding: 12px;
      text-align: center;
    }

    #screen-reports .summary-bottom-item + .summary-bottom-item {
      border-left: 1px solid #e5e7eb;
    }

    #screen-reports .summary-bottom-label {
      font-size: 11px;
      color: var(--text-muted);
      margin-bottom: 3px;
    }

    #screen-reports .summary-bottom-value {
      font-size: 17px;
      font-weight: 800;
      color: var(--text-main);
    }

    #screen-reports .action-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
      margin-bottom: 10px;
    }

    #screen-reports .action-row-1 {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
      margin-bottom: 8px;
    }

    #screen-reports .action-row-2 {
      margin-bottom: 14px;
    }

    #screen-reports .primary-btn,
    #screen-reports .secondary-btn,
    #screen-reports .ghost-btn,
    #screen-reports .danger-btn {
      border-radius: 999px;
      padding: 11px 14px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.05s, box-shadow 0.15s, background 0.15s;
      border: none;
      width: 100%;
    }

    #screen-reports .primary-btn {
      color: #ffffff;
      background: linear-gradient(135deg, var(--blue), var(--blue-dark));
      box-shadow: 0 14px 35px rgba(37, 99, 235, 0.45);
    }

    #screen-reports .secondary-btn {
      color: var(--blue);
      border: 1px solid #bfdbfe;
      background: #eff6ff;
    }

    #screen-reports .ghost-btn {
      color: var(--text-main);
      border: 1px solid #e5e7eb;
      background: #ffffff;
    }

    #screen-reports .danger-btn {
      color: #dc2626;
      border: 1px solid #fecaca;
      background: #fef2f2;
    }

    #screen-reports .primary-btn:active,
    #screen-reports .secondary-btn:active,
    #screen-reports .ghost-btn:active,
    #screen-reports .danger-btn:active {
      transform: scale(0.97);
    }

    #screen-reports .primary-btn:disabled,
    #screen-reports .secondary-btn:disabled,
    #screen-reports .ghost-btn:disabled,
    #screen-reports .danger-btn:disabled {
      opacity: 0.6;
      cursor: not-allowed;
      box-shadow: none;
    }

    #screen-reports .list {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    #screen-reports .semester-item,
    #screen-reports .plan-item {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 14px;
      padding: 10px 10px;
      display: flex;
      gap: 10px;
      align-items: center;
      justify-content: space-between;
    }

    #screen-reports .semester-item-left,
    #screen-reports .plan-item-left {
      flex: 1;
      min-width: 0;
    }

    #screen-reports .semester-item-title,
    #screen-reports .plan-item-title {
      font-size: 15px;
      font-weight: 700;
      color: var(--text-main);
      line-height: 1.25;
      margin-bottom: 3px;
    }

    #screen-reports .semester-item-sub,
    #screen-reports .plan-item-sub {
      font-size: 12px;
      color: var(--text-muted);
      line-height: 1.35;
    }

    #screen-reports .semester-item-right,
    #screen-reports .plan-item-right {
      display: flex;
      align-items: center;
      gap: 6px;
      flex-shrink: 0;
    }

    #screen-reports .tiny-btn {
      border: 1px solid #e5e7eb;
      background: #fff;
      color: var(--text-main);
      border-radius: 999px;
      padding: 7px 10px;
      font-size: 12px;
      font-weight: 600;
      cursor: pointer;
      white-space: nowrap;
    }

    #screen-reports .tiny-btn.select.active {
      background: #eff6ff;
      color: var(--blue);
      border-color: #bfdbfe;
    }

    #screen-reports .tiny-btn.edit {
      background: #ffffff;
    }

    #screen-reports .tiny-btn.open {
      background: #eff6ff;
      color: var(--blue);
      border-color: #bfdbfe;
    }

    #screen-reports .inline-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 8px;
      margin-bottom: 8px;
    }

    #screen-reports .inline-head .section-title {
      margin: 0;
    }

    #screen-reports .inline-action {
      font-size: 12px;
      font-weight: 700;
      color: #dc2626;
      background: none;
      border: none;
      padding: 0;
      cursor: pointer;
    }

    #screen-reports .chips {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    #screen-reports .chip {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: #eff6ff;
      color: var(--blue);
      border: 1px solid #bfdbfe;
      padding: 8px 10px;
      border-radius: 999px;
      font-size: 12px;
      font-weight: 600;
    }

    #screen-reports .chip button {
      border: none;
      background: none;
      color: var(--blue);
      font-size: 14px;
      cursor: pointer;
      padding: 0;
      line-height: 1;
    }

    #screen-reports .field-block {
      margin-bottom: 10px;
    }

    #screen-reports .field-label {
      display: block;
      font-size: 12px;
      font-weight: 600;
      color: var(--text-main);
      margin: 0 0 5px;
    }

    #screen-reports .text-input,
    #screen-reports .select-input {
      width: 100%;
      padding: 11px 12px;
      border-radius: 14px;
      border: 1px solid #e5e7eb;
      font-size: 16px;
      background: #fff;
      color: var(--text-main);
      box-sizing: border-box;
    }

    #screen-reports .text-input:focus,
    #screen-reports .select-input:focus {
      outline: none;
      border-color: var(--blue);
      box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.16);
    }

    #screen-reports .picker-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 8px;
    }

    #screen-reports .picker-item {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 14px;
      padding: 11px 12px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      cursor: pointer;
    }

    #screen-reports .picker-item-title {
      font-size: 14px;
      font-weight: 700;
      color: var(--text-main);
    }

    #screen-reports .picker-item-sub {
      font-size: 11px;
      color: var(--text-muted);
      margin-top: 2px;
      line-height: 1.35;
    }

    #screen-reports .picker-badge {
      border-radius: 999px;
      padding: 7px 10px;
      font-size: 12px;
      font-weight: 700;
      color: var(--blue);
      background: #eff6ff;
      border: 1px solid #bfdbfe;
      white-space: nowrap;
    }

    #screen-reports .picker-badge.done {
      color: #065f46;
      background: #ecfdf5;
      border-color: #bbf7d0;
    }

    #screen-reports .editor-header {
      margin-bottom: 10px;
    }

    #screen-reports .editor-title {
      font-size: 17px;
      font-weight: 800;
      color: var(--text-main);
      margin-bottom: 3px;
    }

    #screen-reports .editor-sub {
      font-size: 12px;
      color: var(--text-muted);
      line-height: 1.45;
    }

    #screen-reports .course-list {
      display: flex;
      flex-direction: column;
      gap: 8px;
      margin-bottom: 12px;
    }

    #screen-reports .course-item {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 14px;
      padding: 10px;
    }

    #screen-reports .course-item-top {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 8px;
    }

    #screen-reports .course-code {
      font-size: 14px;
      font-weight: 800;
      color: var(--text-main);
    }

    #screen-reports .course-title {
      font-size: 12px;
      color: var(--text-muted);
      line-height: 1.35;
      margin-top: 2px;
    }

    #screen-reports .course-meta {
      font-size: 12px;
      color: var(--text-main);
      font-weight: 700;
      white-space: nowrap;
    }

    #screen-reports .course-actions {
      display: flex;
      gap: 6px;
      margin-top: 8px;
    }

    #screen-reports .course-form-grid {
      display: grid;
      grid-template-columns: 1.2fr 0.8fr 0.9fr;
      gap: 8px;
      margin-bottom: 8px;
    }

    #screen-reports .result-stat-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
      margin-bottom: 10px;
    }

    #screen-reports .stat-box {
      border-radius: 16px;
      padding: 12px;
      color: #ffffff;
      min-height: 78px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    #screen-reports .stat-box.orange { background: linear-gradient(135deg, #f97316, #f59e0b); }
    #screen-reports .stat-box.green { background: linear-gradient(135deg, #10b981, #22c55e); }
    #screen-reports .stat-box.purple { background: linear-gradient(135deg, #7c3aed, #6366f1); }
    #screen-reports .stat-box.blue { background: linear-gradient(135deg, #2563eb, #1d4ed8); }

    #screen-reports .stat-label {
      font-size: 12px;
      opacity: 0.95;
      margin-bottom: 4px;
      font-weight: 700;
    }

    #screen-reports .stat-value {
      font-size: 27px;
      font-weight: 800;
      line-height: 1;
    }

    #screen-reports .standing-box {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 16px;
      padding: 14px 12px;
      margin-bottom: 10px;
    }

    #screen-reports .standing-title {
      font-size: 13px;
      font-weight: 700;
      color: var(--text-main);
      margin-bottom: 6px;
    }

    #screen-reports .standing-value {
      font-size: 28px;
      font-weight: 800;
      color: #065f46;
      line-height: 1.1;
      margin-bottom: 4px;
    }

    #screen-reports .standing-sub {
      font-size: 12px;
      color: var(--text-muted);
      line-height: 1.45;
    }

    #screen-reports .breakdown-item {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 14px;
      padding: 10px;
    }

    #screen-reports .breakdown-item + .breakdown-item {
      margin-top: 8px;
    }

    #screen-reports .breakdown-head {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      align-items: center;
      margin-bottom: 4px;
    }

    #screen-reports .breakdown-title {
      font-size: 14px;
      font-weight: 700;
      color: var(--text-main);
    }

    #screen-reports .breakdown-gpa {
      font-size: 14px;
      font-weight: 800;
      color: var(--blue);
      white-space: nowrap;
    }

    #screen-reports .breakdown-sub {
      font-size: 12px;
      color: var(--text-muted);
      line-height: 1.35;
    }

    #screen-reports .target-summary-box {
      background: #ffffff;
      border: 1px solid #e5e7eb;
      border-radius: 16px;
      padding: 12px;
      margin-top: 10px;
    }

    #screen-reports .target-summary-top {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 10px;
      margin-bottom: 8px;
    }

    #screen-reports .target-badge {
      display: inline-flex;
      align-items: center;
      padding: 7px 10px;
      border-radius: 999px;
      font-size: 12px;
      font-weight: 700;
      background: #eff6ff;
      color: var(--blue);
      border: 1px solid #bfdbfe;
    }

    #screen-reports .target-badge.bad {
      background: #fef2f2;
      color: #dc2626;
      border-color: #fecaca;
    }

    #screen-reports .target-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
      margin: 10px 0;
    }

    #screen-reports .target-stat {
      background: #f9fafb;
      border: 1px solid #e5e7eb;
      border-radius: 14px;
      padding: 10px;
    }

    #screen-reports .target-stat-label {
      font-size: 11px;
      color: var(--text-muted);
      margin-bottom: 3px;
    }

    #screen-reports .target-stat-value {
      font-size: 18px;
      font-weight: 800;
      color: var(--text-main);
    }

    #screen-reports .muted-box {
      background: #f9fafb;
      border: 1px dashed #d1d5db;
      border-radius: 14px;
      padding: 10px;
      font-size: 12px;
      color: var(--text-muted);
      line-height: 1.5;
    }

    #screen-reports .empty-state {
      font-size: 12px;
      color: var(--text-muted);
      text-align: center;
      padding: 10px 6px;
      line-height: 1.5;
    }

    #screen-reports .message {
      margin-top: 8px;
      font-size: 12px;
      text-align: center;
      white-space: pre-line;
    }

    #screen-reports .message.error { color: var(--error); }
    #screen-reports .message.success { color: var(--success); }
    #screen-reports .hidden { display: none !important; }

    @media (max-width: 360px) {
      #screen-reports .action-row,
      #screen-reports .action-row-1,
      #screen-reports .result-stat-grid,
      #screen-reports .course-form-grid,
      #screen-reports .target-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>

  <!-- HUB PAGE -->
  <div id="cgpaHubPage">
    <section class="hero">
      <div class="hero-line-1">CGPA</div>
      <h1 class="hero-title">
        CGPA calculator<br />
        <span>Add semesters, calculate result & target plan</span>
      </h1>
    </section>

    <div class="summary-card">
      <div class="summary-top">
        <div>
          <div class="summary-top-label">Current CGPA</div>
          <div id="cgpaCurrentValue" class="summary-top-value">—</div>
        </div>
        <div id="cgpaCurrentStanding" class="summary-standing">No result yet</div>
      </div>
      <div class="summary-bottom">
        <div class="summary-bottom-item">
          <div class="summary-bottom-label">Semesters Saved</div>
          <div id="cgpaSavedCount" class="summary-bottom-value">0</div>
        </div>
        <div class="summary-bottom-item">
          <div class="summary-bottom-label">Total Units</div>
          <div id="cgpaTotalUnits" class="summary-bottom-value">0</div>
        </div>
      </div>
    </div>

    <div class="action-row-1">
      <button id="btnOpenSemesterPicker" class="secondary-btn" type="button">+ Add New Semester</button>
      <button id="btnCalculateCgpa" class="primary-btn" type="button">Calculate CGPA</button>
    </div>

    <div class="action-row-2">
      <button id="btnOpenTargetPlanner" class="ghost-btn" type="button">Target Class Plan</button>
    </div>

    <section>
      <div class="section-title">Saved semesters</div>
      <div class="section-sub">
        Add new semesters, edit old ones, or select any saved semester for calculation.
      </div>

      <div class="card">
        <div id="cgpaSemesterList" class="list"></div>
        <div id="cgpaSemesterEmpty" class="empty-state hidden">
          No semester added yet. Tap <strong>Add New Semester</strong> to start.
        </div>
      </div>
    </section>

    <section>
      <div class="inline-head">
        <div class="section-title">Selected semesters</div>
        <button id="btnClearSelectedSemesters" class="inline-action" type="button">Clear All</button>
      </div>
      <div class="section-sub">
        These are only the semesters selected for the current calculation. Clear All will not delete saved data.
      </div>

      <div class="card">
        <div id="cgpaSelectedChips" class="chips"></div>
        <div id="cgpaSelectedEmpty" class="empty-state">
          No semester selected yet.
        </div>
      </div>
    </section>

    <section>
      <div class="section-title">Saved target plans</div>
      <div class="section-sub">
        Open your previous target class plans and view the saved advice again.
      </div>

      <div class="card">
        <div id="cgpaTargetPlanList" class="list"></div>
        <div id="cgpaTargetPlanEmpty" class="empty-state">
          No target class plan saved yet.
        </div>
      </div>
    </section>

    <div id="cgpaHubError" class="message error hidden"></div>
    <div id="cgpaHubSuccess" class="message success hidden"></div>
  </div>

  <!-- PICKER PAGE -->
  <div id="cgpaPickerPage" class="hidden">
    <section class="hero">
      <div class="hero-line-1">CGPA</div>
      <h1 class="hero-title">
        Choose semester<br />
        <span>Select the semester to add or edit</span>
      </h1>
    </section>

    <div class="card">
      <div class="field-block">
        <label class="field-label" for="cgpaPickerSessionInput">Academic session</label>
        <input id="cgpaPickerSessionInput" type="text" class="text-input" placeholder="e.g. 2024/2025" />
      </div>

      <div class="section-title" style="margin-top:0;">Semester options</div>
      <div class="section-sub">
        Tap any semester from 100 level to 700 level. If it already exists, it will open for editing.
      </div>

      <div id="cgpaPickerList" class="picker-grid"></div>

      <button id="btnBackFromPicker" class="ghost-btn" type="button" style="margin-top:10px;">Back</button>
    </div>

    <div id="cgpaPickerError" class="message error hidden"></div>
  </div>

  <!-- EDITOR PAGE -->
  <div id="cgpaEditorPage" class="hidden">
    <section class="hero">
      <div class="hero-line-1">CGPA</div>
      <h1 class="hero-title">
        Semester editor<br />
        <span>Add courses & save result</span>
      </h1>
    </section>

    <div class="card">
      <div class="editor-header">
        <div id="cgpaEditorSemesterTitle" class="editor-title">Semester</div>
        <div id="cgpaEditorSemesterSub" class="editor-sub">Add all your courses, units and grades here.</div>
      </div>

      <div class="course-form-grid">
        <div class="field-block" style="margin-bottom:0;">
          <label class="field-label" for="cgpaCourseCodeInput">Course code</label>
          <input id="cgpaCourseCodeInput" type="text" class="text-input" placeholder="GST 101" />
        </div>
        <div class="field-block" style="margin-bottom:0;">
          <label class="field-label" for="cgpaCourseUnitInput">Unit</label>
          <input id="cgpaCourseUnitInput" type="number" class="text-input" min="1" step="1" placeholder="2" />
        </div>
        <div class="field-block" style="margin-bottom:0;">
          <label class="field-label" for="cgpaCourseGradeInput">Grade</label>
          <select id="cgpaCourseGradeInput" class="select-input">
            <option value="">Grade</option>
            <option value="A">A</option>
            <option value="B">B</option>
            <option value="C">C</option>
            <option value="D">D</option>
            <option value="E">E</option>
            <option value="F">F</option>
          </select>
        </div>
      </div>

      <div class="field-block">
        <label class="field-label" for="cgpaCourseTitleInput">Course title (optional)</label>
        <input id="cgpaCourseTitleInput" type="text" class="text-input" placeholder="Use of English" />
      </div>

      <div class="action-row">
        <button id="btnAddCourseToSemester" class="secondary-btn" type="button">+ Add Course</button>
        <button id="btnCancelCourseEdit" class="ghost-btn" type="button">Cancel Edit</button>
      </div>

      <div class="section-title">Courses in this semester</div>
      <div id="cgpaCourseList" class="course-list"></div>
      <div id="cgpaCourseEmpty" class="empty-state">No course added yet.</div>

      <div class="section-title">Preview</div>
      <div class="section-sub">This preview is calculated from the courses you have added in this semester.</div>

      <div class="summary-card" style="margin-bottom:10px;">
        <div class="summary-top" style="background: linear-gradient(135deg, #2563eb, #60a5fa);">
          <div>
            <div class="summary-top-label">Semester GPA</div>
            <div id="cgpaEditorPreviewGpa" class="summary-top-value">0.00</div>
          </div>
          <div id="cgpaEditorPreviewStanding" class="summary-standing" style="color:#1d4ed8;">Preview</div>
        </div>
        <div class="summary-bottom">
          <div class="summary-bottom-item">
            <div class="summary-bottom-label">Courses</div>
            <div id="cgpaEditorPreviewCourses" class="summary-bottom-value">0</div>
          </div>
          <div class="summary-bottom-item">
            <div class="summary-bottom-label">Units</div>
            <div id="cgpaEditorPreviewUnits" class="summary-bottom-value">0</div>
          </div>
        </div>
      </div>

      <div class="action-row">
        <button id="btnBackFromEditor" class="ghost-btn" type="button">Back</button>
        <button id="btnSaveSemesterCourses" class="primary-btn" type="button">Save Semester</button>
      </div>
    </div>

    <div id="cgpaEditorError" class="message error hidden"></div>
    <div id="cgpaEditorSuccess" class="message success hidden"></div>
  </div>

  <!-- RESULT PAGE -->
  <div id="cgpaResultPage" class="hidden">
    <section class="hero">
      <div class="hero-line-1">CGPA</div>
      <h1 class="hero-title">
        Result summary<br />
        <span>Calculated from selected semesters</span>
      </h1>
    </section>

    <div class="card">
      <div class="result-stat-grid">
        <div class="stat-box orange">
          <div class="stat-label">Current CGPA</div>
          <div id="cgpaResultCgpa" class="stat-value">0.00</div>
        </div>
        <div class="stat-box green">
          <div class="stat-label">Total Units</div>
          <div id="cgpaResultUnits" class="stat-value">0</div>
        </div>
        <div class="stat-box purple">
          <div class="stat-label">Quality Points</div>
          <div id="cgpaResultQualityPoints" class="stat-value">0</div>
        </div>
        <div class="stat-box blue">
          <div class="stat-label">Semesters Used</div>
          <div id="cgpaResultSemestersUsed" class="stat-value">0</div>
        </div>
      </div>

      <div class="standing-box">
        <div class="standing-title">Class Standing</div>
        <div id="cgpaResultStanding" class="standing-value">—</div>
        <div id="cgpaResultStandingSub" class="standing-sub">Your current class standing will appear here.</div>
      </div>

      <div class="section-title">Selected semesters</div>
      <div id="cgpaResultSelectedList"></div>

      <div class="section-title">Semester breakdown</div>
      <div id="cgpaResultBreakdown"></div>

      <div class="action-row" style="margin-top:12px;">
        <button id="btnBackFromResult" class="ghost-btn" type="button">Back</button>
        <button id="btnCalculateAgain" class="primary-btn" type="button">Calculate Again</button>
      </div>
    </div>

    <div id="cgpaResultError" class="message error hidden"></div>
  </div>

  <!-- TARGET PLANNER PAGE -->
  <div id="cgpaTargetPage" class="hidden">
    <section class="hero">
      <div class="hero-line-1">CGPA</div>
      <h1 class="hero-title">
        Target class plan<br />
        <span>Plan one semester and get short advice</span>
      </h1>
    </section>

    <div class="card">
      <div class="field-block">
        <label class="field-label" for="cgpaTargetLevelInput">Future level</label>
        <select id="cgpaTargetLevelInput" class="select-input">
          <option value="">Select level</option>
          <option value="100">100 Level</option>
          <option value="200">200 Level</option>
          <option value="300">300 Level</option>
          <option value="400">400 Level</option>
          <option value="500">500 Level</option>
          <option value="600">600 Level</option>
          <option value="700">700 Level</option>
        </select>
      </div>

      <div class="field-block">
        <label class="field-label" for="cgpaTargetSemesterInput">Future semester</label>
        <select id="cgpaTargetSemesterInput" class="select-input">
          <option value="">Select semester</option>
          <option value="1">1st Semester</option>
          <option value="2">2nd Semester</option>
        </select>
      </div>

      <div class="field-block">
        <label class="field-label" for="cgpaTargetSessionInput">Academic session</label>
        <input id="cgpaTargetSessionInput" type="text" class="text-input" placeholder="e.g. 2025/2026" />
      </div>

      <div class="field-block">
        <label class="field-label" for="cgpaTargetClassInput">Target class</label>
        <select id="cgpaTargetClassInput" class="select-input">
          <option value="">Select target class</option>
          <option value="First Class">First Class</option>
          <option value="Second Class Upper">Second Class Upper</option>
          <option value="Second Class Lower">Second Class Lower</option>
          <option value="Third Class">Third Class</option>
          <option value="Pass">Pass</option>
        </select>
      </div>

      <div class="section-title">Future semester courses</div>
      <div class="section-sub">
        Add only the course code, title and unit for the semester you want to plan.
      </div>

      <div class="course-form-grid">
        <div class="field-block" style="margin-bottom:0;">
          <label class="field-label" for="cgpaTargetCourseCodeInput">Course code</label>
          <input id="cgpaTargetCourseCodeInput" type="text" class="text-input" placeholder="CSC 201" />
        </div>
        <div class="field-block" style="margin-bottom:0;">
          <label class="field-label" for="cgpaTargetCourseUnitInput">Unit</label>
          <input id="cgpaTargetCourseUnitInput" type="number" class="text-input" min="1" step="1" placeholder="3" />
        </div>
        <div class="field-block" style="margin-bottom:0;">
          <label class="field-label" for="cgpaTargetCourseTitleInput">Title</label>
          <input id="cgpaTargetCourseTitleInput" type="text" class="text-input" placeholder="Data Structures" />
        </div>
      </div>

      <div class="action-row">
        <button id="btnAddTargetCourse" class="secondary-btn" type="button">+ Add Course</button>
        <button id="btnClearTargetCourseForm" class="ghost-btn" type="button">Clear</button>
      </div>

      <div id="cgpaTargetCourseList" class="course-list"></div>
      <div id="cgpaTargetCourseEmpty" class="empty-state">
        No future course added yet.
      </div>

      <div class="action-row" style="margin-top:10px;">
        <button id="btnBackFromTarget" class="ghost-btn" type="button">Back</button>
        <button id="btnGetTargetAdvice" class="primary-btn" type="button">Get Target Advice</button>
      </div>
    </div>

    <div id="cgpaTargetResultBox" class="card hidden">
      <div class="section-title" style="margin-top:0;">Target result</div>
      <div class="target-summary-box">
        <div class="target-summary-top">
          <div>
            <div class="breakdown-title" id="cgpaTargetResultTitle">Target plan</div>
            <div class="breakdown-sub" id="cgpaTargetResultSub">Saved result will appear here.</div>
          </div>
          <div id="cgpaTargetPossibleBadge" class="target-badge">Possible</div>
        </div>

        <div class="target-grid">
          <div class="target-stat">
            <div class="target-stat-label">Current CGPA</div>
            <div id="cgpaTargetCurrentCgpa" class="target-stat-value">0.00</div>
          </div>
          <div class="target-stat">
            <div class="target-stat-label">Required GPA</div>
            <div id="cgpaTargetRequiredGpa" class="target-stat-value">0.00</div>
          </div>
          <div class="target-stat">
            <div class="target-stat-label">Future units</div>
            <div id="cgpaTargetFutureUnits" class="target-stat-value">0</div>
          </div>
          <div class="target-stat">
            <div class="target-stat-label">Target class</div>
            <div id="cgpaTargetClassValue" class="target-stat-value">—</div>
          </div>
        </div>

        <div class="muted-box" id="cgpaTargetAiSummary">Advice will appear here.</div>
      </div>
    </div>

    <div id="cgpaTargetError" class="message error hidden"></div>
    <div id="cgpaTargetSuccess" class="message success hidden"></div>
  </div>

  <!-- TARGET DETAILS PAGE -->
  <div id="cgpaTargetDetailPage" class="hidden">
    <section class="hero">
      <div class="hero-line-1">CGPA</div>
      <h1 class="hero-title">
        Saved target plan<br />
        <span>Open previous target advice</span>
      </h1>
    </section>

    <div class="card">
      <div class="target-summary-box">
        <div class="target-summary-top">
          <div>
            <div class="breakdown-title" id="cgpaDetailTitle">Target plan</div>
            <div class="breakdown-sub" id="cgpaDetailSub">Saved plan details.</div>
          </div>
          <div id="cgpaDetailPossibleBadge" class="target-badge">Possible</div>
        </div>

        <div class="target-grid">
          <div class="target-stat">
            <div class="target-stat-label">Current CGPA</div>
            <div id="cgpaDetailCurrentCgpa" class="target-stat-value">0.00</div>
          </div>
          <div class="target-stat">
            <div class="target-stat-label">Required GPA</div>
            <div id="cgpaDetailRequiredGpa" class="target-stat-value">0.00</div>
          </div>
          <div class="target-stat">
            <div class="target-stat-label">Future units</div>
            <div id="cgpaDetailFutureUnits" class="target-stat-value">0</div>
          </div>
          <div class="target-stat">
            <div class="target-stat-label">Target class</div>
            <div id="cgpaDetailTargetClass" class="target-stat-value">—</div>
          </div>
        </div>

        <div class="muted-box" id="cgpaDetailAiSummary">Saved summary.</div>
      </div>

      <div class="section-title">Future courses</div>
      <div id="cgpaDetailCourseList" class="course-list"></div>

      <button id="btnBackFromTargetDetail" class="ghost-btn" type="button" style="margin-top:10px;">Back</button>
    </div>

    <div id="cgpaTargetDetailError" class="message error hidden"></div>
  </div>

  <script>
    (function () {
      if (window.__cgpaReportsScreenInit) return;
      window.__cgpaReportsScreenInit = true;

      let cgpaSemestersCache = [];
      let cgpaSelectedSemesterIds = [];
      let cgpaCurrentEditorSemester = null;
      let cgpaCurrentEditorCourses = [];
      let cgpaEditingCourseId = null;
      let cgpaLatestSummary = null;

      let cgpaTargetPlansCache = [];
      let cgpaTargetCourses = [];
      let cgpaCurrentTargetPlanId = null;

      function cgpaQ(id) {
        return document.getElementById(id);
      }

      function cgpaShowPage(pageId) {
        [
          "cgpaHubPage",
          "cgpaPickerPage",
          "cgpaEditorPage",
          "cgpaResultPage",
          "cgpaTargetPage",
          "cgpaTargetDetailPage"
        ].forEach((id) => {
          const el = cgpaQ(id);
          if (el) el.classList.toggle("hidden", id !== pageId);
        });
      }

      function cgpaShowMessage(targetId, msg, type) {
        const el = cgpaQ(targetId);
        if (!el) return;
        el.textContent = msg || "";
        el.className = "message " + (type || "error") + (msg ? "" : " hidden");
        if (!msg) el.classList.add("hidden");
      }

      function cgpaClearMessages() {
        [
          "cgpaHubError", "cgpaHubSuccess",
          "cgpaPickerError",
          "cgpaEditorError", "cgpaEditorSuccess",
          "cgpaResultError",
          "cgpaTargetError", "cgpaTargetSuccess",
          "cgpaTargetDetailError"
        ].forEach((id) => cgpaShowMessage(id, ""));
      }

      function cgpaSafeText(v) {
        return String(v || "").trim();
      }

      function cgpaRound2(n) {
        return Math.round((Number(n || 0) + Number.EPSILON) * 100) / 100;
      }

      function cgpaGradePoint(letter) {
        const g = cgpaSafeText(letter).toUpperCase();
        if (g === "A") return 5;
        if (g === "B") return 4;
        if (g === "C") return 3;
        if (g === "D") return 2;
        if (g === "E") return 1;
        return 0;
      }

      function cgpaStandingFromCgpa(cgpa) {
        const c = Number(cgpa || 0);
        if (c >= 4.5) return "First Class";
        if (c >= 3.5) return "Second Class Upper";
        if (c >= 2.4) return "Second Class Lower";
        if (c >= 1.5) return "Third Class";
        if (c >= 1.0) return "Pass";
        return "Fail";
      }

      function cgpaFormatSemester(level, semester, academicSession) {
        const semText = Number(semester) === 2 ? "2nd Semester" : "1st Semester";
        return `${level} Level · ${semText}${academicSession ? ` · ${academicSession}` : ""}`;
      }

      function cgpaFindSemesterById(id) {
        return cgpaSemestersCache.find((s) => String(s.id) === String(id)) || null;
      }

      function cgpaSetSelectedSemesterIds(ids) {
        cgpaSelectedSemesterIds = Array.from(new Set((ids || []).map(String)));
        try {
          localStorage.setItem("colspot_cgpa_selected_semester_ids", JSON.stringify(cgpaSelectedSemesterIds));
        } catch (e) {}
      }

      function cgpaLoadSelectedSemesterIds() {
        try {
          const raw = localStorage.getItem("colspot_cgpa_selected_semester_ids");
          const parsed = raw ? JSON.parse(raw) : [];
          cgpaSelectedSemesterIds = Array.isArray(parsed) ? parsed.map(String) : [];
        } catch (e) {
          cgpaSelectedSemesterIds = [];
        }
      }

      function cgpaToggleSelectedSemester(id) {
        const sid = String(id);
        if (cgpaSelectedSemesterIds.includes(sid)) {
          cgpaSetSelectedSemesterIds(cgpaSelectedSemesterIds.filter((x) => x !== sid));
        } else {
          cgpaSetSelectedSemesterIds([...cgpaSelectedSemesterIds, sid]);
        }
        cgpaRenderHub();
      }

      function cgpaRemoveSelectedSemester(id) {
        const sid = String(id);
        cgpaSetSelectedSemesterIds(cgpaSelectedSemesterIds.filter((x) => x !== sid));
        cgpaRenderHub();
      }

      function cgpaClearAllSelectedSemesters() {
        cgpaSetSelectedSemesterIds([]);
        cgpaRenderHub();
        cgpaShowMessage("cgpaHubSuccess", "Selected semesters cleared.", "success");
      }

      function cgpaResetTargetForm() {
        cgpaCurrentTargetPlanId = null;
        cgpaTargetCourses = [];
        if (cgpaQ("cgpaTargetLevelInput")) cgpaQ("cgpaTargetLevelInput").value = "";
        if (cgpaQ("cgpaTargetSemesterInput")) cgpaQ("cgpaTargetSemesterInput").value = "";
        if (cgpaQ("cgpaTargetSessionInput")) cgpaQ("cgpaTargetSessionInput").value = "";
        if (cgpaQ("cgpaTargetClassInput")) cgpaQ("cgpaTargetClassInput").value = "";
        if (cgpaQ("cgpaTargetCourseCodeInput")) cgpaQ("cgpaTargetCourseCodeInput").value = "";
        if (cgpaQ("cgpaTargetCourseUnitInput")) cgpaQ("cgpaTargetCourseUnitInput").value = "";
        if (cgpaQ("cgpaTargetCourseTitleInput")) cgpaQ("cgpaTargetCourseTitleInput").value = "";
        cgpaQ("cgpaTargetResultBox")?.classList.add("hidden");
        cgpaRenderTargetCourseList();
      }

      function cgpaRenderTargetCourseList() {
        const list = cgpaQ("cgpaTargetCourseList");
        const empty = cgpaQ("cgpaTargetCourseEmpty");
        if (!list) return;
        list.innerHTML = "";

        if (!cgpaTargetCourses.length) {
          empty?.classList.remove("hidden");
          return;
        }

        empty?.classList.add("hidden");

        cgpaTargetCourses.forEach((course, idx) => {
          const row = document.createElement("div");
          row.className = "course-item";
          row.innerHTML = `
            <div class="course-item-top">
              <div>
                <div class="course-code">${cgpaSafeText(course.course_code)}</div>
                <div class="course-title">${cgpaSafeText(course.course_title) || "No title added"}</div>
              </div>
              <div class="course-meta">${Number(course.unit || 0)} unit(s)</div>
            </div>
            <div class="course-actions">
              <button class="tiny-btn remove" type="button" data-target-course-remove="${idx}">Remove</button>
            </div>
          `;
          list.appendChild(row);
        });

        list.querySelectorAll("[data-target-course-remove]").forEach((btn) => {
          btn.addEventListener("click", () => {
            const idx = Number(btn.getAttribute("data-target-course-remove"));
            cgpaTargetCourses.splice(idx, 1);
            cgpaRenderTargetCourseList();
          });
        });
      }

      async function cgpaEnsureUser() {
        try {
          if (typeof ensureCurrentUser === "function") {
            const user = await ensureCurrentUser();
            if (user && typeof currentUser !== "undefined") currentUser = user;
            return user;
          }
        } catch (e) {}

        if (typeof supabase === "undefined") return null;

        try {
          const { data, error } = await supabase.auth.getUser();
          if (error || !data?.user) return null;
          if (typeof currentUser !== "undefined") currentUser = data.user;
          return data.user;
        } catch (e) {
          return null;
        }
      }

      async function cgpaInvokeCalculator(body) {
        return await supabase.functions.invoke("cgpa-calculate", { body });
      }

      async function cgpaLoadSemesters() {
        cgpaClearMessages();

        if (typeof supabase === "undefined") {
          cgpaShowMessage("cgpaHubError", "Backend not available. Please reload and try again.");
          return;
        }

        const user = await cgpaEnsureUser();
        if (!user) {
          cgpaShowMessage("cgpaHubError", "Your session has expired. Please sign in again.");
          return;
        }

        const { data, error } = await supabase
          .from("cgpa_semesters")
          .select("id, user_id, level, semester, academic_session, is_selected, created_at, updated_at")
          .eq("user_id", user.id)
          .order("level", { ascending: true })
          .order("semester", { ascending: true })
          .order("academic_session", { ascending: true });

        if (error) {
          cgpaShowMessage("cgpaHubError", error.message || "Could not load semesters. Please try again.");
          return;
        }

        cgpaSemestersCache = Array.isArray(data) ? data : [];
        cgpaSelectedSemesterIds = cgpaSelectedSemesterIds.filter((id) => cgpaFindSemesterById(id));
        cgpaSetSelectedSemesterIds(cgpaSelectedSemesterIds);

        await cgpaLoadHubSummary();
        await cgpaLoadTargetPlans();
        cgpaRenderHub();
      }

      async function cgpaLoadHubSummary() {
        if (!cgpaSemestersCache.length) {
          cgpaLatestSummary = null;
          cgpaRenderSummaryCard();
          return;
        }

        const allIds = cgpaSemestersCache.map((s) => s.id);

        try {
          const { data, error } = await cgpaInvokeCalculator({
            mode: "calculate",
            semester_ids: allIds
          });

          if (error || !data?.success) {
            cgpaLatestSummary = null;
            cgpaRenderSummaryCard();
            return;
          }

          cgpaLatestSummary = data;
          cgpaRenderSummaryCard();
        } catch (e) {
          cgpaLatestSummary = null;
          cgpaRenderSummaryCard();
        }
      }

      async function cgpaLoadTargetPlans() {
        const list = cgpaQ("cgpaTargetPlanList");
        const empty = cgpaQ("cgpaTargetPlanEmpty");
        if (list) list.innerHTML = "";

        try {
          const { data, error } = await cgpaInvokeCalculator({ mode: "list_target_plans" });

          if (error || !data?.success) {
            cgpaTargetPlansCache = [];
            empty?.classList.remove("hidden");
            return;
          }

          cgpaTargetPlansCache = Array.isArray(data.plans) ? data.plans : [];

          if (!cgpaTargetPlansCache.length) {
            empty?.classList.remove("hidden");
            return;
          }

          empty?.classList.add("hidden");

          cgpaTargetPlansCache.forEach((plan) => {
            const item = document.createElement("div");
            item.className = "plan-item";
            item.innerHTML = `
              <div class="plan-item-left">
                <div class="plan-item-title">${cgpaFormatSemester(plan.future_level, plan.future_semester, plan.future_academic_session || "")}</div>
                <div class="plan-item-sub">${cgpaSafeText(plan.target_class)} • Required GPA ${Number(plan.required_future_gpa || 0).toFixed(2)}</div>
              </div>
              <div class="plan-item-right">
                <button class="tiny-btn open" type="button" data-plan-open="${plan.id}">Open</button>
              </div>
            `;
            list?.appendChild(item);
          });

          list?.querySelectorAll("[data-plan-open]").forEach((btn) => {
            btn.addEventListener("click", () => {
              const planId = btn.getAttribute("data-plan-open");
              if (planId) cgpaOpenTargetPlanDetail(planId);
            });
          });
        } catch (e) {
          cgpaTargetPlansCache = [];
          empty?.classList.remove("hidden");
        }
      }

      function cgpaRenderSummaryCard() {
        const currentVal = cgpaQ("cgpaCurrentValue");
        const standingVal = cgpaQ("cgpaCurrentStanding");
        const savedCount = cgpaQ("cgpaSavedCount");
        const totalUnits = cgpaQ("cgpaTotalUnits");

        if (savedCount) savedCount.textContent = String(cgpaSemestersCache.length);

        if (!cgpaLatestSummary?.summary) {
          if (currentVal) currentVal.textContent = "—";
          if (standingVal) standingVal.textContent = cgpaSemestersCache.length ? "Tap Calculate CGPA" : "No result yet";
          if (totalUnits) totalUnits.textContent = "0";
          return;
        }

        const summary = cgpaLatestSummary.summary;
        if (currentVal) currentVal.textContent = Number(summary.cgpa || 0).toFixed(2);
        if (standingVal) standingVal.textContent = summary.class_standing || "—";
        if (totalUnits) totalUnits.textContent = String(summary.total_units || 0);
      }

      function cgpaRenderHub() {
        cgpaRenderSummaryCard();

        const list = cgpaQ("cgpaSemesterList");
        const empty = cgpaQ("cgpaSemesterEmpty");
        const chips = cgpaQ("cgpaSelectedChips");
        const chipsEmpty = cgpaQ("cgpaSelectedEmpty");

        if (list) list.innerHTML = "";
        if (chips) chips.innerHTML = "";

        if (!cgpaSemestersCache.length) {
          empty?.classList.remove("hidden");
        } else {
          empty?.classList.add("hidden");

          cgpaSemestersCache.forEach((sem) => {
            const selected = cgpaSelectedSemesterIds.includes(String(sem.id));
            const item = document.createElement("div");
            item.className = "semester-item";
            item.innerHTML = `
              <div class="semester-item-left">
                <div class="semester-item-title">${cgpaFormatSemester(sem.level, sem.semester, sem.academic_session)}</div>
                <div class="semester-item-sub" id="semesterSub_${sem.id}">Loading details...</div>
              </div>
              <div class="semester-item-right">
                <button class="tiny-btn select ${selected ? "active" : ""}" data-semester-select="${sem.id}" type="button">
                  ${selected ? "Selected" : "Select"}
                </button>
                <button class="tiny-btn edit" data-semester-edit="${sem.id}" type="button">Edit</button>
              </div>
            `;
            list?.appendChild(item);
            cgpaFillSemesterCardSub(sem.id);
          });
        }

        const selectedItems = cgpaSelectedSemesterIds
          .map((id) => cgpaFindSemesterById(id))
          .filter(Boolean);

        if (!selectedItems.length) {
          chipsEmpty?.classList.remove("hidden");
        } else {
          chipsEmpty?.classList.add("hidden");
          selectedItems.forEach((sem) => {
            const chip = document.createElement("div");
            chip.className = "chip";
            chip.innerHTML = `
              <span>${cgpaFormatSemester(sem.level, sem.semester, "").replace(" · ", " · ")}</span>
              <button type="button" data-chip-remove="${sem.id}">×</button>
            `;
            chips?.appendChild(chip);
          });
        }

        list?.querySelectorAll("[data-semester-select]").forEach((btn) => {
          btn.addEventListener("click", () => cgpaToggleSelectedSemester(btn.getAttribute("data-semester-select")));
        });

        list?.querySelectorAll("[data-semester-edit]").forEach((btn) => {
          btn.addEventListener("click", () => cgpaOpenEditor(btn.getAttribute("data-semester-edit")));
        });

        chips?.querySelectorAll("[data-chip-remove]").forEach((btn) => {
          btn.addEventListener("click", () => cgpaRemoveSelectedSemester(btn.getAttribute("data-chip-remove")));
        });
      }

      async function cgpaFillSemesterCardSub(semesterId) {
        const sub = cgpaQ("semesterSub_" + semesterId);
        if (!sub) return;

        try {
          const { data, error } = await cgpaInvokeCalculator({
            mode: "calculate",
            semester_ids: [semesterId]
          });

          if (error || !data?.success || !data?.semester_breakdown?.length) {
            sub.textContent = "Could not load GPA.";
            return;
          }

          const item = data.semester_breakdown[0];
          const count = Array.isArray(item.courses) ? item.courses.length : 0;
          sub.textContent = `${count} course${count === 1 ? "" : "s"} • GPA ${Number(item.gpa || 0).toFixed(2)}`;
        } catch (e) {
          sub.textContent = "Could not load GPA.";
        }
      }

      function cgpaRenderPicker() {
        const list = cgpaQ("cgpaPickerList");
        if (!list) return;
        list.innerHTML = "";

        const sessionVal = cgpaSafeText(cgpaQ("cgpaPickerSessionInput")?.value);

        [100, 200, 300, 400, 500, 600, 700].forEach((level) => {
          [1, 2].forEach((semester) => {
            const existing = cgpaSemestersCache.find(
              (s) =>
                Number(s.level) === level &&
                Number(s.semester) === semester &&
                cgpaSafeText(s.academic_session) === sessionVal
            );

            const row = document.createElement("div");
            row.className = "picker-item";
            row.innerHTML = `
              <div>
                <div class="picker-item-title">${cgpaFormatSemester(level, semester, sessionVal)}</div>
                <div class="picker-item-sub">${existing ? "Already saved for this session. Tap to open and edit." : "Tap to create and add courses."}</div>
              </div>
              <div class="picker-badge ${existing ? "done" : ""}">${existing ? "Open" : "Add"}</div>
            `;
            row.addEventListener("click", () => cgpaCreateOrOpenSemester(level, semester, sessionVal));
            list.appendChild(row);
          });
        });
      }

      async function cgpaCreateOrOpenSemester(level, semester, academicSession) {
        cgpaShowMessage("cgpaPickerError", "");

        if (!academicSession) {
          cgpaShowMessage("cgpaPickerError", "Please enter academic session first.");
          return;
        }

        let existing = cgpaSemestersCache.find(
          (s) =>
            Number(s.level) === Number(level) &&
            Number(s.semester) === Number(semester) &&
            cgpaSafeText(s.academic_session) === cgpaSafeText(academicSession)
        );

        if (existing) {
          cgpaOpenEditor(existing.id);
          return;
        }

        const user = await cgpaEnsureUser();
        if (!user) {
          cgpaShowMessage("cgpaPickerError", "Your session has expired. Please sign in again.");
          return;
        }

        const { data, error } = await supabase
          .from("cgpa_semesters")
          .insert([{
            user_id: user.id,
            level: Number(level),
            semester: Number(semester),
            academic_session: academicSession,
            is_selected: true
          }])
          .select()
          .single();

        if (error) {
          cgpaShowMessage("cgpaPickerError", error.message || "Could not create semester.");
          return;
        }

        cgpaSemestersCache.push(data);
        cgpaSetSelectedSemesterIds([...cgpaSelectedSemesterIds, data.id]);
        await cgpaLoadHubSummary();
        cgpaOpenEditor(data.id);
      }

      async function cgpaOpenEditor(semesterId) {
        cgpaClearMessages();

        const semester = cgpaFindSemesterById(semesterId);
        if (!semester) {
          cgpaShowMessage("cgpaHubError", "Semester not found.");
          return;
        }

        cgpaCurrentEditorSemester = semester;
        cgpaEditingCourseId = null;
        cgpaEditorResetForm();

        const title = cgpaQ("cgpaEditorSemesterTitle");
        const sub = cgpaQ("cgpaEditorSemesterSub");
        if (title) title.textContent = cgpaFormatSemester(semester.level, semester.semester, semester.academic_session);
        if (sub) sub.textContent = "Add all your courses, units and grades for this semester, then save.";

        const { data, error } = await supabase
          .from("cgpa_courses")
          .select("id, semester_id, user_id, course_code, course_title, unit, grade_letter, created_at, updated_at")
          .eq("semester_id", semester.id)
          .order("created_at", { ascending: true });

        if (error) {
          cgpaShowMessage("cgpaEditorError", error.message || "Could not load courses for this semester.");
          return;
        }

        cgpaCurrentEditorCourses = Array.isArray(data) ? data : [];
        cgpaRenderEditorCourses();
        cgpaShowPage("cgpaEditorPage");
      }

      function cgpaEditorResetForm() {
        cgpaQ("cgpaCourseCodeInput").value = "";
        cgpaQ("cgpaCourseTitleInput").value = "";
        cgpaQ("cgpaCourseUnitInput").value = "";
        cgpaQ("cgpaCourseGradeInput").value = "";
        cgpaEditingCourseId = null;
      }

      function cgpaRenderEditorCourses() {
        const list = cgpaQ("cgpaCourseList");
        const empty = cgpaQ("cgpaCourseEmpty");

        if (list) list.innerHTML = "";

        if (!cgpaCurrentEditorCourses.length) {
          empty?.classList.remove("hidden");
        } else {
          empty?.classList.add("hidden");

          cgpaCurrentEditorCourses.forEach((course) => {
            const gp = cgpaGradePoint(course.grade_letter);
            const qp = Number(course.unit || 0) * gp;
            const div = document.createElement("div");
            div.className = "course-item";
            div.innerHTML = `
              <div class="course-item-top">
                <div>
                  <div class="course-code">${cgpaSafeText(course.course_code) || "Course"}</div>
                  <div class="course-title">${cgpaSafeText(course.course_title) || "No title added"}</div>
                </div>
                <div class="course-meta">${Number(course.unit || 0)} unit(s) · ${cgpaSafeText(course.grade_letter)} · ${cgpaRound2(qp)} pts</div>
              </div>
              <div class="course-actions">
                <button class="tiny-btn edit" data-course-edit="${course.id}" type="button">Edit</button>
                <button class="tiny-btn remove" data-course-delete="${course.id}" type="button">Delete</button>
              </div>
            `;
            list?.appendChild(div);
          });
        }

        list?.querySelectorAll("[data-course-edit]").forEach((btn) => {
          btn.addEventListener("click", () => cgpaLoadCourseIntoForm(btn.getAttribute("data-course-edit")));
        });

        list?.querySelectorAll("[data-course-delete]").forEach((btn) => {
          btn.addEventListener("click", () => cgpaDeleteCourse(btn.getAttribute("data-course-delete")));
        });

        cgpaRenderEditorPreview();
      }

      function cgpaRenderEditorPreview() {
        const totalCourses = cgpaCurrentEditorCourses.length;
        let totalUnits = 0;
        let qualityPoints = 0;

        cgpaCurrentEditorCourses.forEach((course) => {
          const unit = Number(course.unit || 0);
          const gp = cgpaGradePoint(course.grade_letter);
          totalUnits += unit;
          qualityPoints += unit * gp;
        });

        const gpa = totalUnits > 0 ? qualityPoints / totalUnits : 0;

        cgpaQ("cgpaEditorPreviewCourses").textContent = String(totalCourses);
        cgpaQ("cgpaEditorPreviewUnits").textContent = String(cgpaRound2(totalUnits));
        cgpaQ("cgpaEditorPreviewGpa").textContent = Number(gpa).toFixed(2);
        cgpaQ("cgpaEditorPreviewStanding").textContent = cgpaStandingFromCgpa(gpa);
      }

      function cgpaLoadCourseIntoForm(courseId) {
        const course = cgpaCurrentEditorCourses.find((c) => String(c.id) === String(courseId));
        if (!course) return;

        cgpaEditingCourseId = course.id;
        cgpaQ("cgpaCourseCodeInput").value = cgpaSafeText(course.course_code);
        cgpaQ("cgpaCourseTitleInput").value = cgpaSafeText(course.course_title);
        cgpaQ("cgpaCourseUnitInput").value = String(course.unit || "");
        cgpaQ("cgpaCourseGradeInput").value = cgpaSafeText(course.grade_letter).toUpperCase();
        cgpaShowMessage("cgpaEditorSuccess", "Course loaded for editing.", "success");
      }

      async function cgpaAddOrUpdateCourse() {
        cgpaShowMessage("cgpaEditorError", "");
        cgpaShowMessage("cgpaEditorSuccess", "");

        if (!cgpaCurrentEditorSemester?.id) {
          cgpaShowMessage("cgpaEditorError", "No semester is open.");
          return;
        }

        const course_code = cgpaSafeText(cgpaQ("cgpaCourseCodeInput")?.value);
        const course_title = cgpaSafeText(cgpaQ("cgpaCourseTitleInput")?.value);
        const unit = Number(cgpaQ("cgpaCourseUnitInput")?.value || 0);
        const grade_letter = cgpaSafeText(cgpaQ("cgpaCourseGradeInput")?.value).toUpperCase();
        const user = await cgpaEnsureUser();

        if (!user) {
          cgpaShowMessage("cgpaEditorError", "Your session has expired. Please sign in again.");
          return;
        }
        if (!course_code) {
          cgpaShowMessage("cgpaEditorError", "Enter course code.");
          return;
        }
        if (!(unit > 0)) {
          cgpaShowMessage("cgpaEditorError", "Enter a valid course unit.");
          return;
        }
        if (!["A", "B", "C", "D", "E", "F"].includes(grade_letter)) {
          cgpaShowMessage("cgpaEditorError", "Choose a valid grade.");
          return;
        }

        const payload = {
          semester_id: cgpaCurrentEditorSemester.id,
          user_id: user.id,
          course_code,
          course_title: course_title || null,
          unit,
          grade_letter
        };

        if (cgpaEditingCourseId) {
          const { data, error } = await supabase
            .from("cgpa_courses")
            .update(payload)
            .eq("id", cgpaEditingCourseId)
            .select()
            .single();

          if (error) {
            cgpaShowMessage("cgpaEditorError", error.message || "Could not update course.");
            return;
          }

          cgpaCurrentEditorCourses = cgpaCurrentEditorCourses.map((c) =>
            String(c.id) === String(cgpaEditingCourseId) ? data : c
          );
          cgpaEditorResetForm();
          cgpaRenderEditorCourses();
          cgpaShowMessage("cgpaEditorSuccess", "Course updated.", "success");
          return;
        }

        const { data, error } = await supabase
          .from("cgpa_courses")
          .insert([payload])
          .select()
          .single();

        if (error) {
          cgpaShowMessage("cgpaEditorError", error.message || "Could not add course.");
          return;
        }

        cgpaCurrentEditorCourses.push(data);
        cgpaEditorResetForm();
        cgpaRenderEditorCourses();
        cgpaShowMessage("cgpaEditorSuccess", "Course added.", "success");
      }

      async function cgpaDeleteCourse(courseId) {
        if (!confirm("Delete this course from the semester?")) return;

        const { error } = await supabase
          .from("cgpa_courses")
          .delete()
          .eq("id", courseId);

        if (error) {
          cgpaShowMessage("cgpaEditorError", error.message || "Could not delete course.");
          return;
        }

        cgpaCurrentEditorCourses = cgpaCurrentEditorCourses.filter((c) => String(c.id) !== String(courseId));
        cgpaRenderEditorCourses();
        cgpaShowMessage("cgpaEditorSuccess", "Course deleted.", "success");
      }

      async function cgpaSaveSemesterAndBack() {
        if (!cgpaCurrentEditorSemester?.id) {
          cgpaShowMessage("cgpaEditorError", "No semester is open.");
          return;
        }
        if (!cgpaCurrentEditorCourses.length) {
          cgpaShowMessage("cgpaEditorError", "Add at least one course before saving.");
          return;
        }

        if (!cgpaSelectedSemesterIds.includes(String(cgpaCurrentEditorSemester.id))) {
          cgpaSetSelectedSemesterIds([...cgpaSelectedSemesterIds, String(cgpaCurrentEditorSemester.id)]);
        }

        await cgpaLoadSemesters();
        cgpaShowPage("cgpaHubPage");
        cgpaShowMessage("cgpaHubSuccess", "Semester saved successfully.", "success");
      }

      async function cgpaCalculateSelected() {
        cgpaShowMessage("cgpaHubError", "");
        cgpaShowMessage("cgpaResultError", "");

        if (!cgpaSelectedSemesterIds.length) {
          cgpaShowMessage("cgpaHubError", "Select at least one semester.");
          return;
        }

        try {
          const { data, error } = await cgpaInvokeCalculator({
            mode: "calculate",
            semester_ids: cgpaSelectedSemesterIds
          });

          if (error || !data?.success) {
            cgpaShowMessage("cgpaHubError", data?.error || error?.message || "Could not calculate CGPA.");
            return;
          }

          cgpaRenderResult(data);
          cgpaShowPage("cgpaResultPage");
        } catch (e) {
          cgpaShowMessage("cgpaHubError", "Could not calculate CGPA.");
        }
      }

      function cgpaRenderResult(data) {
        const summary = data?.summary || {};
        const breakdown = Array.isArray(data?.semester_breakdown) ? data.semester_breakdown : [];

        cgpaQ("cgpaResultCgpa").textContent = Number(summary.cgpa || 0).toFixed(2);
        cgpaQ("cgpaResultUnits").textContent = String(summary.total_units || 0);
        cgpaQ("cgpaResultQualityPoints").textContent = String(summary.total_quality_points || 0);
        cgpaQ("cgpaResultSemestersUsed").textContent = String(summary.total_semesters || breakdown.length || 0);

        cgpaQ("cgpaResultStanding").textContent = summary.class_standing || "—";
        cgpaQ("cgpaResultStandingSub").textContent =
          summary.class_standing
            ? `This result was calculated from ${summary.total_semesters || breakdown.length} selected semester(s).`
            : "Your current class standing will appear here.";

        const selectedList = cgpaQ("cgpaResultSelectedList");
        const breakdownList = cgpaQ("cgpaResultBreakdown");
        selectedList.innerHTML = "";
        breakdownList.innerHTML = "";

        breakdown.forEach((item) => {
          const selected = document.createElement("div");
          selected.className = "breakdown-item";
          selected.innerHTML = `
            <div class="breakdown-head">
              <div class="breakdown-title">${cgpaFormatSemester(item.level, item.semester, item.academic_session)}</div>
              <div class="breakdown-gpa">GPA ${Number(item.gpa || 0).toFixed(2)}</div>
            </div>
            <div class="breakdown-sub">${item.total_units || 0} unit(s) • ${Array.isArray(item.courses) ? item.courses.length : 0} course(s)</div>
          `;
          selectedList.appendChild(selected);
        });

        breakdown.forEach((item) => {
          const row = document.createElement("div");
          row.className = "breakdown-item";
          row.innerHTML = `
            <div class="breakdown-head">
              <div class="breakdown-title">${cgpaFormatSemester(item.level, item.semester, item.academic_session)}</div>
              <div class="breakdown-gpa">GPA ${Number(item.gpa || 0).toFixed(2)}</div>
            </div>
            <div class="breakdown-sub">
              ${item.total_units || 0} unit(s) • ${item.total_quality_points || 0} quality point(s)
            </div>
          `;
          breakdownList.appendChild(row);
        });
      }

      async function cgpaAddTargetCourse() {
        cgpaShowMessage("cgpaTargetError", "");
        const course_code = cgpaSafeText(cgpaQ("cgpaTargetCourseCodeInput").value);
        const course_title = cgpaSafeText(cgpaQ("cgpaTargetCourseTitleInput").value);
        const unit = Number(cgpaQ("cgpaTargetCourseUnitInput").value || 0);

        if (!course_code) {
          cgpaShowMessage("cgpaTargetError", "Enter future course code.");
          return;
        }
        if (!(unit > 0)) {
          cgpaShowMessage("cgpaTargetError", "Enter a valid future course unit.");
          return;
        }

        cgpaTargetCourses.push({
          course_code,
          course_title,
          unit
        });

        cgpaQ("cgpaTargetCourseCodeInput").value = "";
        cgpaQ("cgpaTargetCourseTitleInput").value = "";
        cgpaQ("cgpaTargetCourseUnitInput").value = "";
        cgpaRenderTargetCourseList();
      }

      function cgpaRenderTargetResult(targetPlan, currentSummary) {
        cgpaQ("cgpaTargetResultBox").classList.remove("hidden");
        cgpaQ("cgpaTargetResultTitle").textContent =
          `${cgpaFormatSemester(targetPlan.future_level, targetPlan.future_semester, targetPlan.future_academic_session || "")}`;
        cgpaQ("cgpaTargetResultSub").textContent =
          `Target: ${targetPlan.target_class}`;

        cgpaQ("cgpaTargetCurrentCgpa").textContent = Number(currentSummary.cgpa || 0).toFixed(2);
        cgpaQ("cgpaTargetRequiredGpa").textContent = Number(targetPlan.required_future_gpa || 0).toFixed(2);
        cgpaQ("cgpaTargetFutureUnits").textContent = String(targetPlan.future_total_units || 0);
        cgpaQ("cgpaTargetClassValue").textContent = cgpaSafeText(targetPlan.target_class) || "—";

        const badge = cgpaQ("cgpaTargetPossibleBadge");
        badge.textContent = targetPlan.possible === false ? "Hard / Not Possible" : "Possible";
        badge.className = "target-badge" + (targetPlan.possible === false ? " bad" : "");

        cgpaQ("cgpaTargetAiSummary").textContent =
          `${cgpaSafeText(targetPlan.ai_summary)}${targetPlan.score_hint ? " " + cgpaSafeText(targetPlan.score_hint) : ""}`;
      }

      async function cgpaGetTargetAdvice() {
        cgpaShowMessage("cgpaTargetError", "");
        cgpaShowMessage("cgpaTargetSuccess", "");

        if (!cgpaSelectedSemesterIds.length) {
          cgpaShowMessage("cgpaTargetError", "Select at least one real semester first on the main page.");
          return;
        }

        const future_level = Number(cgpaQ("cgpaTargetLevelInput").value || 0);
        const future_semester = Number(cgpaQ("cgpaTargetSemesterInput").value || 0);
        const future_academic_session = cgpaSafeText(cgpaQ("cgpaTargetSessionInput").value);
        const target_class = cgpaSafeText(cgpaQ("cgpaTargetClassInput").value);

        if (![100, 200, 300, 400, 500, 600, 700].includes(future_level)) {
          cgpaShowMessage("cgpaTargetError", "Choose a valid future level.");
          return;
        }
        if (![1, 2].includes(future_semester)) {
          cgpaShowMessage("cgpaTargetError", "Choose a valid future semester.");
          return;
        }
        if (!target_class) {
          cgpaShowMessage("cgpaTargetError", "Choose a target class.");
          return;
        }
        if (!cgpaTargetCourses.length) {
          cgpaShowMessage("cgpaTargetError", "Add at least one future course.");
          return;
        }

        try {
          const { data, error } = await cgpaInvokeCalculator({
            mode: "target_plan",
            semester_ids: cgpaSelectedSemesterIds,
            target_class,
            future_level,
            future_semester,
            future_academic_session,
            future_courses: cgpaTargetCourses,
            save_plan: true
          });

          if (error || !data?.success) {
            cgpaShowMessage("cgpaTargetError", data?.error || error?.message || "Could not generate target advice.");
            return;
          }

          cgpaCurrentTargetPlanId = data?.target_plan?.id || null;
          cgpaRenderTargetResult(data.target_plan, data.current_summary || {});
          await cgpaLoadTargetPlans();
          cgpaShowMessage("cgpaTargetSuccess", "Target class plan saved successfully.", "success");
        } catch (e) {
          cgpaShowMessage("cgpaTargetError", "Could not generate target advice.");
        }
      }

      async function cgpaOpenTargetPlanDetail(planId) {
        cgpaShowMessage("cgpaTargetDetailError", "");

        try {
          const { data, error } = await cgpaInvokeCalculator({
            mode: "get_target_plan",
            target_plan_id: planId
          });

          if (error || !data?.success || !data?.target_plan) {
            cgpaShowMessage("cgpaTargetDetailError", data?.error || error?.message || "Could not open target plan.");
            return;
          }

          const plan = data.target_plan;
          cgpaQ("cgpaDetailTitle").textContent =
            `${cgpaFormatSemester(plan.future_level, plan.future_semester, plan.future_academic_session || "")}`;
          cgpaQ("cgpaDetailSub").textContent = `Target: ${cgpaSafeText(plan.target_class)}`;

          cgpaQ("cgpaDetailCurrentCgpa").textContent = Number(plan.current_cgpa || 0).toFixed(2);
          cgpaQ("cgpaDetailRequiredGpa").textContent = Number(plan.required_future_gpa || 0).toFixed(2);
          cgpaQ("cgpaDetailFutureUnits").textContent = String(plan.future_total_units || 0);
          cgpaQ("cgpaDetailTargetClass").textContent = cgpaSafeText(plan.target_class) || "—";
          cgpaQ("cgpaDetailAiSummary").textContent = cgpaSafeText(plan.ai_summary) || "No summary saved.";

          const badge = cgpaQ("cgpaDetailPossibleBadge");
          badge.textContent = plan.possible === false ? "Hard / Not Possible" : "Possible";
          badge.className = "target-badge" + (plan.possible === false ? " bad" : "");

          const courseList = cgpaQ("cgpaDetailCourseList");
          courseList.innerHTML = "";

          const futureCourses = Array.isArray(plan.future_courses) ? plan.future_courses : [];
          if (!futureCourses.length) {
            const empty = document.createElement("div");
            empty.className = "empty-state";
            empty.textContent = "No future courses saved.";
            courseList.appendChild(empty);
          } else {
            futureCourses.forEach((course) => {
              const row = document.createElement("div");
              row.className = "course-item";
              row.innerHTML = `
                <div class="course-item-top">
                  <div>
                    <div class="course-code">${cgpaSafeText(course.course_code)}</div>
                    <div class="course-title">${cgpaSafeText(course.course_title) || "No title added"}</div>
                  </div>
                  <div class="course-meta">${Number(course.unit || 0)} unit(s)</div>
                </div>
              `;
              courseList.appendChild(row);
            });
          }

          cgpaShowPage("cgpaTargetDetailPage");
        } catch (e) {
          cgpaShowMessage("cgpaTargetDetailError", "Could not open target plan.");
        }
      }

      function cgpaGoHub() {
        cgpaShowPage("cgpaHubPage");
      }

      function cgpaBindEvents() {
        cgpaQ("btnOpenSemesterPicker")?.addEventListener("click", () => {
          cgpaShowMessage("cgpaPickerError", "");
          cgpaRenderPicker();
          cgpaShowPage("cgpaPickerPage");
        });

        cgpaQ("btnCalculateCgpa")?.addEventListener("click", cgpaCalculateSelected);
        cgpaQ("btnClearSelectedSemesters")?.addEventListener("click", cgpaClearAllSelectedSemesters);

        cgpaQ("btnOpenTargetPlanner")?.addEventListener("click", () => {
          cgpaClearMessages();
          cgpaResetTargetForm();
          cgpaShowPage("cgpaTargetPage");
        });

        cgpaQ("btnBackFromPicker")?.addEventListener("click", cgpaGoHub);
        cgpaQ("cgpaPickerSessionInput")?.addEventListener("input", cgpaRenderPicker);

        cgpaQ("btnAddCourseToSemester")?.addEventListener("click", cgpaAddOrUpdateCourse);
        cgpaQ("btnCancelCourseEdit")?.addEventListener("click", () => {
          cgpaEditorResetForm();
          cgpaShowMessage("cgpaEditorSuccess", "Course edit cancelled.", "success");
        });

        cgpaQ("btnBackFromEditor")?.addEventListener("click", cgpaGoHub);
        cgpaQ("btnSaveSemesterCourses")?.addEventListener("click", cgpaSaveSemesterAndBack);

        cgpaQ("btnBackFromResult")?.addEventListener("click", cgpaGoHub);
        cgpaQ("btnCalculateAgain")?.addEventListener("click", cgpaGoHub);

        cgpaQ("btnBackFromTarget")?.addEventListener("click", cgpaGoHub);
        cgpaQ("btnAddTargetCourse")?.addEventListener("click", cgpaAddTargetCourse);
        cgpaQ("btnClearTargetCourseForm")?.addEventListener("click", () => {
          cgpaQ("cgpaTargetCourseCodeInput").value = "";
          cgpaQ("cgpaTargetCourseUnitInput").value = "";
          cgpaQ("cgpaTargetCourseTitleInput").value = "";
        });
        cgpaQ("btnGetTargetAdvice")?.addEventListener("click", cgpaGetTargetAdvice);

        cgpaQ("btnBackFromTargetDetail")?.addEventListener("click", cgpaGoHub);
      }

      async function cgpaInit() {
        cgpaLoadSelectedSemesterIds();
        cgpaBindEvents();
        cgpaShowPage("cgpaHubPage");
        await cgpaLoadSemesters();
      }

      if (document.readyState === "loading") {
        document.addEventListener("DOMContentLoaded", cgpaInit);
      } else {
        cgpaInit();
      }
    })();
  </script>
</div>
